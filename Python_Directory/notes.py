# we will learn about file and directory management

# a directory is collection of files and subdirectories
# python has os module to work with file and directory

# Get current directory in python

import os

print(os.getcwd())

# getcwd() retrun present directory in the form of string
